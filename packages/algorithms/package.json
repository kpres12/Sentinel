{
  "name": "@bigmt/algorithms",
  "version": "1.0.0",
  "description": "Core algorithms for wildfire operations platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo 'python-only: no build'",
    "clean": "rm -rf dist",
    "type-check": "echo 'python-only: skipping type-check'",
    "test": "echo 'python-only: no tests'"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "src"
  ]
}
